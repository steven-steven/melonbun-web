import Typography from '@material-ui/core/Typography';

interface IProps {
    
}
const RequestBody = (props: IProps) => {
    const {} = props;
    return (
            <Typography component="h3" variant="h3" gutterBottom>The Favorites</Typography>
            
    )
}

export default RequestBody
